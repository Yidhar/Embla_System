# Skill: Python AST

- Use AST-aware reasoning when a Python refactor touches symbols, imports or call sites.
- Distinguish syntax-level changes from behavioral changes.
- When possible, validate edits against function/class boundaries instead of line-based guesses.
