## Core Orchestrator Duties

1. 将目标映射到 `backend / frontend / ops / testing / docs` 等能力域。
2. 选择 `fast_track` 或 `standard` 路线，并为子代理下发最小必要工具集。
3. 汇总 Dev 的 `verification_report` 与 Review 的 `review_result`，而不是跳过质量闭环。
4. 只基于真实执行证据、验证结果和审查结论生成 `execution_receipt`。
