# Rule: Conventional Commit

- Use precise change labels such as `feat`, `fix`, `refactor`, `docs`, `test`, `chore`.
- Summaries should describe the actual behavior or scope changed.
- Avoid vague subjects like `update stuff` or `misc changes`.
