# NagaAgent 依赖（一键打包 build.py 与 PyInstaller 均使用此文件）
# Python 建议 3.11

# ---------- 核心 / 配置 ----------
charset-normalizer>=3.4.0
json5>=0.9.0
PyYAML>=6.0.2
pydantic>=2.11.0
requests>=2.32.0

# ---------- Web 服务 ----------
fastapi>=0.115.0
uvicorn>=0.34.0
starlette
sse-starlette>=2.3.0
httpx>=0.28.0
httpcore
websockets>=15.0

# ---------- LLM / 对话 ----------
openai>=2.8.0
litellm>=1.80.0
tiktoken>=0.11.0
langchain-openai>=1.1.0
langchain-community>=0.3.30

# ---------- 异步 / 任务 ----------
aiohttp>=3.12.0
anyio
APScheduler>=3.11.0

# ---------- 系统 / 工具 ----------
numpy>=2.2.0
pandas>=2.2.0
pillow>=11.0.0
psutil>=7.1.0
emoji>=2.14.0
cryptography>=46.0.0
setproctitle>=1.3.5
watchfiles>=1.0.0

# ---------- 语音 / 音频 ----------
edge-tts>=7.0.0
sounddevice>=0.5.0

# ---------- 知识图谱 GRAG / Neo4j（可选，启用 grag 时需要） ----------
py2neo>=2021.2.3
neo4j>=5.0.0

# ---------- nagaagent_core / 兼容层 ----------
Flask>=3.1.0
gevent>=25.0.0

# ---------- MCP 核心（已禁用：main 不再启动 mcpserver） ----------
# mcp>=1.15.0
# redis>=5.2.0

# ---------- MCP 技能 / 可选（已禁用） ----------
# datasets>=3.6.0
# pdfminer.six>=20250506
# polars>=1.37.0
# pyarrow>=19.0.0
# pypdfium2>=4.30.0
# tokenizers>=0.21.0
# python-docx>=1.2.0
# fastmcp>=2.12.0

# ---------- MQTT（已禁用：main 不再启动 mqtt_tool） ----------
# paho-mqtt>=2.1.0

# ---------- 打包用（执行 python build.py 或 pyinstaller 时需安装） ----------
# pyinstaller>=6.0.0
